# =============================================================================
# Manufacturing Docs Pipeline - Environment Variables
# =============================================================================
# 이 파일을 .env로 복사하고 값을 설정하세요:
#   cp .env.example .env
#
# ⚠️ .env 파일은 절대 Git에 커밋하지 마세요!
# =============================================================================

# -----------------------------------------------------------------------------
# AI API Keys (필수)
# -----------------------------------------------------------------------------

# Anthropic Claude API Key
# https://console.anthropic.com/settings/keys 에서 발급
ANTHROPIC_API_KEY=sk-ant-api03-...

# Google Gemini API Key
# https://aistudio.google.com/app/apikey 에서 발급
GOOGLE_API_KEY=AI...

# -----------------------------------------------------------------------------
# Application Settings (선택)
# -----------------------------------------------------------------------------

# 로그 레벨: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 템플릿 저장 경로 (Docker 내부 경로)
TEMPLATES_DIR=/app/templates

# 작업 데이터 저장 경로 (Docker 내부 경로)
JOBS_DIR=/app/jobs

# -----------------------------------------------------------------------------
# Server Settings (선택)
# -----------------------------------------------------------------------------

# 서버 호스트 (기본값: 0.0.0.0)
# HOST=0.0.0.0

# 서버 포트 (기본값: 8000)
# PORT=8000

# 워커 수 (프로덕션용, 기본값: 1)
# WORKERS=4
